#include <iostream>
using namespace std;
int main() {
    int arr[] = {10,3,2,5,10},n = sizeof(arr)/sizeof(arr[0]),largest = arr[0];
    for(int i = 0;i < n;i++){
        largest = max(arr[i],largest);
    }
    cout<<largest;
    return 0;
}
