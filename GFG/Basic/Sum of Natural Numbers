class Solution {
  public:
    int findSum(int n) {
        int sum = 0;
        
        sum = (n * (n+1)) / 2;
        return sum;
    }
};

class Solution {
    public static int findSum(int n) {
        
        int sum = 0;
        sum = (n * (n + 1)) / 2;
        return sum;
    }
}
